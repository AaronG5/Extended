services:
  django:
    build: .
    expose:
      - "8000"
    volumes:
      - ./extended:/app
    env_file:
      - .env
    networks:
      - app_network

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - django
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
